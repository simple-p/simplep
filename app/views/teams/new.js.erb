$('#dialog h3').html("<i class='glyphicon glyphicon-plus'></i> New Team");

// Render the new form
$('.modal-body').html('<%= j render("teams/form") %>');

// Show the dynamic dialog
$('#dialog').modal("show");

// Set focus to the first element
$('#dialog').on('shown.bs.modal', function () {
		$('.first_input').focus()
	})
