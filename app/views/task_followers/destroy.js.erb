
<% if @task.persisted? %>
    $(".followers").html("<%= j render 'tasks/followers', task: @task %>")
<% else %>
    $(".followers").append("Error: #{@task_follower.errors.full_messages.to_sentence}")
<% end %>
